<slot></slot>

<style>
  :global( body, html ) {
    padding: 0;
    margin: 0;
    min-height: 100vh;
    min-width: 100vw;
    background-color: var(--main-bg-color);
    color: var(--main-font-color);
    
  }

  *,
  *::before,
  *::after { 
    box-sizing: border-box;
  }

</style>